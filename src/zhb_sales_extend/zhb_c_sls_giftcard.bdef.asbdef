projection;
strict ( 2 );
use draft;

define behavior for ZHB_C_SLS_GIFTCARD alias Giftcard
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}